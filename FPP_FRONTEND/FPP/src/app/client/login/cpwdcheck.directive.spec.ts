import { CpwdcheckDirective } from './cpwdcheck.directive';

describe('CpwdcheckDirective', () => {
  it('should create an instance', () => {
    const directive = new CpwdcheckDirective();
    expect(directive).toBeTruthy();
  });
});
