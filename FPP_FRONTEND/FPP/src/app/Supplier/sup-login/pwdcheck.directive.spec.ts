import { PwdcheckDirective } from './pwdcheck.directive';

describe('PwdcheckDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdcheckDirective();
    expect(directive).toBeTruthy();
  });
});
